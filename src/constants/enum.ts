export enum UserRole {
  Admin,
  Manager,
  User
}

export enum UserGender {
  Male,
  Female,
  Other
}

export enum UserStatus {
  Unactive,
  Active
}

export enum TokenType {
  AccessToken,
  RefreshToken,
  EmailVerifyToken,
  ForgotPasswordToken
}
