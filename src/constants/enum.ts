enum UserRole {
  Admin,
  Manager,
  User
}

enum UserGender {
  Male,
  Female,
  Other
}

enum UserVerifyStatus {
  Unverified,
  Verified
}

enum UserStatus {
  Unactive,
  Active
}

enum TokenType {
  AccessToken,
  RefreshToken,
  EmailVerifyToken,
  ForgotPasswordToken
}
